<h1>Here are the Students</h1>
<% @student.each do |student| %>
  <div class='student'>
    <%= link_to student.name, student_path(student[:id]) %> - <%= student.house.name %>
    <img src="<%= student.img_url %>">
  </div>
<% end %>
